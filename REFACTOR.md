1. create an "analysis" directory and move the notebooks in there
2. create a "scripts" directory for one off scripts that are used to setup project i.e `data_moving.py`
3. Move benchmark and profiling into scripts
4. Fix the figures for mlflow logging so it's easier to interpret
5. Create quick and dirty baseline model single file script
6. tidy up the experiment naming for mlflow, right now we have like 3 experiments